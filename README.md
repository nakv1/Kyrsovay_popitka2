# StegoTool — Java-приложение для стеганографии изображений

## 📌 Описание проекта
Учебный проект на **Java**: приложение с графическим интерфейсом (Swing), которое позволяет **скрывать текст в изображении методом LSB**, **извлекать его обратно**, а также **передавать изображения по сети** через **TCP-сервер**.

> Идея: изображение выглядит как обычное, но внутри его пикселей хранится скрытое сообщение.
## Возможности

- ✅ Загрузка изображения-контейнера (**PNG, BMP**)
- ✅ Встраивание текстового сообщения методом **LSB**
- ✅ Извлечение скрытого сообщения
- ✅ Проверка вместимости (если сообщение не помещается — операция не выполняется)
- ✅ Сохранение результата (stego-изображение)
- ✅ Клиент–серверный обмен изображениями по **TCP-сокетам**
- ✅ Автосохранение полученных изображений:
  - `downloads/client/` — на стороне клиента
  - `downloads/server/` — на стороне сервера
 
    
## Почему именно PNG/BMP

Метод LSB меняет **младшие биты** цветовых каналов. Форматы **без потерь** (PNG/BMP) сохраняют эти биты при сохранении файла. Так как изменяются только младшие биты, визуально изображение практически не отличается от оригинала.

## Как работает LSB в этом проекте

**LSB (Least Significant Bit)** — младший значащий бит каждого цветового канала пикселя.

- Каждый пиксель содержит каналы **R, G, B** (0–255).
- В каждый канал можно спрятать **1 бит** → всего **3 бита на пиксель**.
- Изменение младшего бита меняет значение канала максимум на ±1, визуально это почти незаметно.

### Формат данных внутри изображения
[ 32 бита длины сообщения (в байтах) ] + [ сообщение (UTF-8), бит за битом ]

Так извлечение всегда знает, сколько данных читать.

---

## Сетевая часть (как обмениваются участники)

Используется **TCP**.

Протокол простой и надёжный:
1) Клиент кодирует изображение в PNG и получает массив байтов  
2) Отправляет:
   - `int` — длина массива (4 байта)
   - `byte[]` — сами данные изображения
3) Сервер принимает изображение, сохраняет его и рассылает подключённым клиентам (кроме отправителя)

Порт сервера по умолчанию: **12345**.

---

## Требования

- **Java 8+**
- Любая IDE (рекомендуется IntelliJ IDEA)
- ОС: Windows

---

## Быстрый старт (запуск)

1. Открой проект в IntelliJ IDEA (или другой IDE)
2. Запусти сервер:
   - класс: `com.example.stego.net.server.Server`
3. Запусти клиент:
   - класс: `com.example.stego.ui.Main`
4. Подключи клиента к серверу (host/port), затем можно отправлять изображения

